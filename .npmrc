# React Native / Metro resolve modules from a flat node_modules tree.
# pnpm's default isolated (symlinked) layout trips Metro up, so use a hoisted
# layout for this RN monorepo. See: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
